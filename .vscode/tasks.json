{
    "tasks": [
        {
            "type": "shell",
            "label": "cftp debug",
            "command": "/usr/bin/make",
            "args": ["debug"],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ],
    "version": "2.0.0"
}